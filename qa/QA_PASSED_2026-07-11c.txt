QA_PASSED
Project 23 -- A reproducible workflow and source-provenance audit for forecasting short
national health-indicator panels: a worked example from Ghana using WHO Global Health
Observatory and World Bank data
Date: 2026-07-11 (third pass -- final full 5-advisor council + technical QA sweep across
all deliverables, supersedes QA_PASSED_2026-07-11b / archived)

Trigger: operator instruction "run a final epid council and QA to scrutinise everything
manuscript, dashboard, poster. everything must align and be consistent and factual" --
treated as a full-manuscript sweep (Trigger B) explicitly extended to the dashboard and
poster narrative text, not just the manuscript body.

Self-directed technical QA sweep (Step 1, before any advisor convened):
  - Citation-key cross-check: 25/25 \cite{} keys resolve to references.bib entries,
    zero orphans, zero broken citations, clean first-appearance order for unsrt style.
  - Every headline number (abstract, README, dashboard, poster) independently
    recomputed against the actual output CSVs / a live model re-fit, not trusted from
    memory: all matched exactly.
  - One real defect found before advisors convened: lasim2022 mislabelled "regional"
    when its actual scope (Cape Coast Metropolis) is metropolitan/municipal-tier.

Full 5-advisor council (roles adapted to this project's actual content -- time-series/
ML methodology and reproducibility, not spatial epidemiology or clinical intervention,
which this paper has neither of): Synthesis Lead, Scite Skeptic, Time-Series & ML
Methods Auditor, Ghana Health-Data Contextualist, Reproducibility & Implementation
Auditor. All five spawned independently in parallel against the live files, not a
pre-digested summary.

7 findings confirmed and fixed as executable artefacts (not documented intentions):

  1. CRITICAL -- malaria ARIMA-vs-exponential-smoothing gap-narrowing was misattributed
     to order selection in Results/Discussion/README/poster. Independently recomputed
     on a like-for-like (log-scale) basis: order selection alone WIDENS the gap
     (3.5% -> 9.1%); the real ~24% -> ~9% narrowing is overwhelmingly a fitting-SCALE
     effect (raw-scale naive fit 24.4% gap -> log-scale-only fit 3.5% gap). Fixed in
     results.md/results_body.tex, discussion.md/discussion_body.tex, README.md, and
     stage12_build_dashboard_poster.py (now reports and correctly attributes all three
     points: naive raw-scale, log-scale-fixed-order, log-scale-own-order).
  2. MODERATE-HIGH -- "decisive" (ΔAICc>2) overstated evidentiary strength relative to
     the paper's own cited Burnham & Anderson convention. Fixed with one calibration
     sentence in Methods; zero tables/counts/tests touched (grep-confirmed no test
     asserts on the literal word).
  3. MODERATE -- full-panel exponential-smoothing forecasting (all 21 series, not just
     the 2 walk-forward-validation series) was real and correctly computed but never
     documented in Methods. One clarifying sentence added.
  4. LOW-MODERATE -- lasim2022 relabelled "a second, metropolitan-level audit" in
     Limitations, matching ayisah2025's correct granularity and distinguishing both
     from piu2024's genuinely region-scale study.
  5. LOW -- two citation claims (aryee2018 "Accra teaching hospital", inusah2026
     "ARIMA model") softened to what the bibliographic titles actually confirm.
  6. LOW-MODERATE -- undocumented orphan script (stage5_national_forecasts_2030.py,
     a pre-AICc-fix script that writes to the same output path as the canonical
     pipeline) added to README's exclusion note with an explicit overwrite-risk
     warning.
  7. LOW -- stale commit-hash self-reference in submission_checklist.md replaced with
     an evergreen pointer to the repository's own Actions tab.

Findings surfaced but judged adequately handled already (reasoned, not silently
dropped): abstract's structural-break hedging judged sufficient as written; Table 4
footnote (a)'s counter-intuitive sign on 2 non-convergent-baseline rows already
disclosed by the existing footnote; missing spatial-analysis dependencies in
requirements.txt judged too low-value (those scripts are already out of scope).

Cross-check discipline: every fix applied to BOTH the .md file (the actual docx source,
via stage10_build_docx.py's add_md_section()) AND the parallel .tex file (LaTeX/PDF
source) -- verified stage10_build_docx.py reads exclusively from .md, so a .tex-only
fix would have silently shipped a wrong docx.

Post-fix verification:
  - Docx rebuilt; grep-verified all 10 new/changed passages present in the compiled
    output, including a clean LaTeX-math-to-Unicode conversion spot-check ("ΔAICc>10").
  - Dashboard/poster regenerated; curl-verified the corrected malaria paragraph renders
    exactly as written; QR SVG path still intact.
  - Citation cross-check re-run: still 25/25, zero orphans.
  - Tenet-20 sync-check re-run: 10/14 PASS; all 4 FAILs re-confirmed as the same
    pre-existing checker false positives logged in the two prior QA passes (no new
    drift introduced).
  - Test suite: 7/7 passing.
  - LaTeX brace-balance check on all 6 edited .tex files: no structural corruption.

5/5 deliverables verified present, current, and mutually consistent (manuscript docx,
poster HTML, dashboard HTML, master CSV, GitHub repo).

Known, disclosed, non-blocking gap (unchanged from prior badges): no Zenodo/OSF
archival DOI yet -- requires the author's own account/action, correctly disclosed as
"in preparation" throughout rather than as an ambiguous to-do note.

Full council findings and Chairman-equivalent synthesis: see AIPOCH_Learning_Log.md,
Project 23 section "FINAL FULL 5-ADVISOR COUNCIL + TECHNICAL QA SWEEP, ALL
DELIVERABLES" dated 2026-07-11.
